# Single Responsibility Responsibility

- An object should have only one primary responsibility. Other things should be moved out of it.

Example:

A journal can have functions to add or remove journal entries. Saving a journal to disk or retrieving a journal are not primary 
responsibilities so they should be moved out of journal.

